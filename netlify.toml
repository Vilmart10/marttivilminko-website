[build]
  command   = "npm install && pip install piexif Pillow opencv-python --quiet && node generate-manifest.js"
  publish   = "."

[build.environment]
  NODE_VERSION   = "20"
  PYTHON_VERSION = "3.11"
